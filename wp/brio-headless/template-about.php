<?php
/**
 * Template Name: About
 *
 * Nothing renders this. It exists so the page can be tagged in Page
 * Attributes, which is what selects the about field group — binding a
 * field group to a page ID instead would break on the staging clone.
 */

defined( 'ABSPATH' ) || exit;

require get_template_directory() . '/index.php';
